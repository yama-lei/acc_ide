jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.1-open
  
install:
  - ./gradlew :treesitter-core:publishToMavenLocal :completion-library:publishToMavenLocal :executor-library:publishToMavenLocal

